tests:
  test_name:
    metrics:
      - "FaithfulnessMetric"
    thresholds:
      - .5
    test_inputs: 
      input: "Is this document relevant to the context?"
      retrieval_context: 
        - "This is the retrieval context"
      context: 
        - "This is the context"
      actual_output: "Yes"
    scenario:
      - scenario_name: "evaluation"