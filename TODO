------------------
* MAJOR NEXT STEPS
1) add a QA element to more dynamically check agent responses
2) add explanations of how insights and decisions have been arrived at. This will require some
basic graph structure.
3) NEW external data sources have to be added.

POSSIBLY USEFUL FEATURES
------------------------

*) Make it so that you can choose not jsut hte top challenge but any number that are within
the "top-right" section which is configurable, or top 2,3 etc.
*) Add checkpointing - use intermediate files to pick up at any certain point.


* open ISSUES / BUGS
--------

*) Dealing with this type of interaction is critical :

It appears that there was an error in accessing the basic information file for NAG or Numerical Algorithms Group with the provided ID 'file-EffK3cLvyQVD5pPrrL4802fl'. The file might not have been correctly uploaded. Could you please re-upload the file or provide the correct file ID?

User
2 Mar 2024, 07:09
Please ensure a file-ID is produced for the JSON file that contains your completed task.

There is real information flow here that is being ignored by the system which is focused only on
JSON file production. Fixing this is critical and would require a LLM overseer or QA intermediary
which could add a lot of overhead
*) Each agent normally takes a while to work out the JSON structure, so we would save time and money by
having a well defined JSON schema that is passed to every agent from the outset, this would also allow more
rigorous checking of results.
*) The timeout code has been removed for debug purposes, but this may mean that certain agents can take
an unreasonable amount of time to complete their task. An agent that doesn't respond favourably in a given timeframe
should be killed and re-run. One lagging agent now will slow down the entire job (cost should not be affected)
*) Important: The agents can only handle 10 files at once, which means that the insight files that are sent
to the trends and capabilities have to be either limited to <9 or this call has to  be split and aggregated. For
now there are max 8 market reports in the market folder.
*) There is inconsistency in the agents, sometimes they don't deliver the output in json and as a file
*) The JSON structure created at the initial stage is inconistent. Let's define a JSON schema that is
then used commonly across all file types. This will save a lot of time and tokens in working out how
the document is structured
*) Need to manage data files differently from insight files.
*) Trend radar - probably need to change from plotly. Matplotlib is not better. This could be that going to R
   provides more robust visual tooling than Python
*) Capabilities - needs some visual tweaks
*) Expanding data sources - need to understand how to capture more real data and how this data is not subject to
   being processed away. Go with the original distinction between insights and data - the latter is not something
   that should be heavily processed
*) Expert reports : introduce agents that will create a summary report on a specialist topic. For example
   financial analyst, strategic analyst, governance analyst, leadership analyst
*) Use run_steps for more information?
*) Parallelism - each agent can actually handle many files, so what is the ideal splitting?
*) Parallelism for improved quality: we can send many agents the same data and aggregate results
*) Testing and improving the quality of the document processing, e.g. retrieval, condensing, splitting, insight generation etc.
*) Polling - report on the steps and then respond back to agent depending on the responses

POTENTIALLY FIXED ISSUES

*) BUG: basic company data is not being correctly passed to agents (potentially fixed 2 Mar)
*) intermediate files should now be working correctly